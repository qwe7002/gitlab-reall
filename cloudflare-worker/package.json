{
  "name": "reall-push",
  "version": "1.0.0",
  "description": "Cloudflare Worker bridging GitLab webhooks to APNs for the Reall iOS app.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
